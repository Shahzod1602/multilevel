services:
  app:
    build: .
    container_name: multilevel-app
    restart: unless-stopped
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"
    networks:
      - multilevel_web

networks:
  multilevel_web:
    external: true
