/**
 * Client-side configuration.
 * Loaded before API and App so values are available at boot.
 */
const Config = {
    apiBaseUrl: 'https://multilevel.identify.uz',
    botUsername: 'MultilevelSpeakingbot',
    deepLinkScheme: 'multilevelspeak',
};
